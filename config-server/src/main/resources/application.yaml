server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/TaeJu/spring-personal-app-config
          clone-on-start: true
  profiles:
    active: native